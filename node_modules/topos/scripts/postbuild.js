#!/usr/bin/env node
'use strict';

var postLayoutToCDB = require('../lib/postLayoutToCDB'),
    deployEnv = require('environment-paypal');


var stageName = process.env.STAGE;

if (deployEnv.isNotDev()) {
    console.log('TOPO postLayoutToCDB: env', process.env.DEPLOY_ENV, 'stageName', stageName);
    postLayoutToCDB();
}
