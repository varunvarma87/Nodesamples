#!/usr/bin/env node
'use strict';

var preLayoutToCDB = require('../lib/preLayoutToCDB'),
    deployEnv = require('environment-paypal');


var stageName = process.env.STAGE;


console.log('TOPO preLayoutToCDB: env', process.env.DEPLOY_ENV, 'stageName', stageName);

if (deployEnv.isNotDev()) {
    preLayoutToCDB();
}
