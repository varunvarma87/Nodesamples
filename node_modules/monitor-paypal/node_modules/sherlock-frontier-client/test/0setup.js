'use strict';

var os = require('os'),
    async = require('async'),
    assert = require('assert'),
    shell = require('shelljs');
//    frontierMockServer = require('./fixtures/mockFrontierServer');

    // Install a copy below fixtures so bytebuffer is available to us and
    // to protobufjs
console.log('\nChanged working dir to -->', __dirname);
console.log('Executing the Npm install for test fixtures ....');
shell.exec('pwd');
shell.exec('cd ' + __dirname + 'test/fixtures;npm install');
