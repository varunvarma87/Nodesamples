'use strict';

module.exports = function (app) {

    app.get('/hello', function (req, res) {
        res.send('hello world');
    });

    app.get('/now:val=', function (req, res) {
    });

    app.all('/now', function (req, res) {
    });
};
