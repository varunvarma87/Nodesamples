'use strict';

var pkg = require('../package.json'),
    debuglog = require('debuglog')(pkg.name);

module.exports = {

    debug: function () {
        debuglog.apply(debuglog, arguments);
    }

};
